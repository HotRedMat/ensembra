{
  "name": "ensembra",
  "version": "0.7.0",
  "description": "Multi-agent orchestrator plugin for Claude Code — agents perform in concert through a 5-phase pipeline (Gather → Deliberate → Execute → Audit → Document) with Reuse-First cross-cutting policy.",
  "author": {
    "name": "Seungho Lee",
    "email": "misstal80@gmail.com"
  },
  "homepage": "https://github.com/HotRedMat/ensembra",
  "repository": "https://github.com/HotRedMat/ensembra",
  "license": "MIT",
  "keywords": [
    "orchestrator",
    "multi-agent",
    "deliberation",
    "review",
    "audit",
    "reuse-first",
    "handover"
  ],
  "mcpServers": {
    "gemini-architect": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-servers/gemini-architect/server.py"],
      "env": {
        "GEMINI_API_KEY": "${user_config.gemini_api_key}"
      }
    }
  },
  "userConfig": {
    "gemini_api_key": {
      "type": "string",
      "title": "Gemini API key (used by MCP server for architect transport in v0.7.0+)",
      "description": "Google AI Studio API key. In v0.7.0+ this key is passed to the ensembra-gemini-architect MCP server via env var — skill/agent content never sees it (sensitive:true invariant). Set via /plugin → ensembra → Configure options. Used when architect transport is configured as 'mcp'.",
      "sensitive": true
    },
    "ollama_endpoint": {
      "type": "string",
      "title": "Ollama endpoint",
      "description": "Ollama HTTP endpoint for the security and qa performers. Default: http://localhost:11434. Leave blank to skip Ollama — those performers will fall back to Claude sub-agents.",
      "sensitive": false
    }
  }
}
